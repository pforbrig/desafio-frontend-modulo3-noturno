import { makeStyles } from "@material-ui/core";

export default makeStyles((theme) => ({
    root: {
        flexGrow: 1,
    },
    paper: {
        height: 433,
        width: 232,
    },
    delete: {
        borderRadius: 200,
        color: '#000000',
        width: 48,
        borderRadius: 48,
    }
}));